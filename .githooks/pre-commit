#!/usr/bin/env bash
set -e
if grep -R -E '"builds"\s*:|"\s*use"\s*:\s*"@vercel/|now-' . --exclude-dir=.githooks; then
  echo "❌ Legacy Vercel builders detected. Remove them before committing."
  exit 1
fi
echo "✅ Vercel config clean"